// var fs = require('fs');

// var os = require('os');

// var _ = require('lodash');   //normally we use _ to require lodash, we can use another name also.

// var user = os.userInfo();



// Username of the operating system user (Unix: getpwuid, Win32: GetUserName)
// console.log(user.username); 


// creates a new file or if it already existsadd the data.

// fs.appendFile('greeting.txt', 'hi '+user.username + '!',()=>{
//     console.log("File is created.")
// });


// using the lodash library

// var data = ['a','a','b','c',1,2,3,4,'a',4,6,8,1];

// var filter = _.uniq(data);   // returns a unique data;

// console.log(filter); 

// console.log(_.isString('Pranjal'));
// console.log(_.isString(3));



// const note = require('./notes.js')  //to  import another file in this file.

// var age = note.age;

// var add = note.addnumber(5,5)

// console.log(age);

// console.log(add)